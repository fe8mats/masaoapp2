/* eslint-disable no-console */
import MsApp from './msapp';

// DOM
const mainContainer = document.getElementById('mainContainer');


// Initialize MasaoApp main program
const msapp = new MsApp(mainContainer);
